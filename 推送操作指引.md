# 推送到 GitHub 并自动打包 APK · 操作指引

> 结论先说：**不需要把账号密码告诉任何人**。GitHub 早已不接受密码推送，
> 必须用 **Personal Access Token（个人访问令牌）**。
> 脚本 `一键推送到GitHub.bat` 已帮你配置好除「建仓库 / 登录」之外的全部步骤。

---

## 第 0 步：确认 GitHub 能访问

刚才实测 `github.com` 返回 200、1.3 秒，**网络已恢复正常**。
之前"打不开"多半是临时波动，重试即可；若浏览器仍打不开，换手机热点或
在 `C:\Windows\System32\drivers\etc\hosts` 加 GitHub 的可用 IP。

---

## 第 1 步：创建空仓库（浏览器里 30 秒）

1. 浏览器打开 <https://github.com/new>（用 `smxderxl` 登录）
2. **Repository name** 填：`iqplay-android`
3. **不要**勾选 Add a README / .gitignore / license —— 保持**空仓库**
4. 点 **Create repository**

> 如果名字想换，记得同步改 `一键推送到GitHub.bat` 里的两处 URL。

---

## 第 2 步：创建 Personal Access Token（只做一次）

1. 打开 <https://github.com/settings/tokens/new>
2. **Note** 随便填，比如 `iqplay-push`
3. **Expiration** 选 30 天或 90 天（到期要重建）
4. **Select scopes** 勾上 **`repo`**（整条勾上，它会自动包含下面子项）
5. 拉到底点 **Generate token**
6. **立刻复制**那串 `ghp_...`（离开页面就再也看不到）

---

## 第 3 步：双击运行 `一键推送到GitHub.bat`

脚本会依次：进入项目目录 → 配置 remote → 切到 main 分支 → 执行 push。

推送时若弹出登录框：
- **Username**：`smxderxl`
- **Password**：粘贴刚才的 **Token**（`ghp_...`，**不是**账号密码）

> Windows 上凭据会被「凭据管理器」记住，之后同一台机器再推就不用重输了。

---

## 第 4 步：等云端自动打包，下载 APK

推送成功后打开：
<https://github.com/smxderxl/iqplay-android/actions>

- 会自动触发 `Build Android APK` 工作流（在 GitHub 的 Ubuntu 机器上现装
  Python 3.11 + buildozer 来构建；不再使用 Docker 镜像）
- 首次约 20~40 分钟（要下载 Android SDK/NDK，还要完整编译 hostpython3 /
  python3 / numpy；之后有缓存会快很多）
- 跑完后点进那次运行，页面底部 **Artifacts** 里下载 `iqplay-apk`
- 解压得到 `.apk`，传到手机安装即可

---

## 万一推送失败

| 报错 | 原因 | 处理 |
|---|---|---|
| `Repository not found` | 仓库没建 / 名字拼错 | 回第 1 步，确认名字是 `iqplay-android` |
| `Authentication failed` | 用了账号密码而非 Token | 回第 2 步，用 `ghp_...` 当密码 |
| `remote: Permission denied` | Token 没勾 `repo` | 重建 Token 并勾上 `repo` |
| 卡住 / 超时 | 网络波动 | 直接重跑脚本 |
| `could not lock config file` | 并发的 git 进程占用 | 关掉其他 git 窗口重试 |

---

## 不想推送？手机直接跑（免 APK、免 GitHub）

同目录已有 `IQ信号分析仪_安卓版_手机直跑版.zip`：
装 **Pydroid 3** → Pip 装 `numpy`、`kivy` → 解压到同一目录 → 打开 `main.py` 运行。
五个页签（频谱/瀑布/余晖/时域/调制）直接可用。

---

## 关于密码（务必看这一条）

- **不要**把 GitHub 账号密码发给任何人、任何 AI。
- 已泄露或曾发出的密码，建议立刻登录 GitHub → **Settings → Password and authentication → Change password** 更换。
- 推送只需 Token，Token 可随时在 <https://github.com/settings/tokens> 撤销，风险远低于密码。
