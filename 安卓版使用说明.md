# IQ 信号综合分析仪 · 安卓版

桌面版 `iqplay0911.py`（tkinter）没法在安卓上跑——tkinter 在安卓没有可用实现，B210 的
UHD 驱动也不存在。所以这一版把**界面层用 Kivy 重写**，**算法层原封不动复用**。

---

## 一、文件清单

| 文件 | 说明 |
|---|---|
| `main.py` | **打包入口**（p4a/buildozer 固定找这个名字），只做转发，不含逻辑 |
| `iqplay_android.py` | 安卓版主程序（Kivy 界面），桌面调试就直接跑它 |
| `iqdsp.py` | 算法层（纯 numpy，由 `_extract_dsp.py` 自动从 `iqplay0911.py` 抽取） |
| `buildozer.spec` | 打包 APK 的配置 |
| `打包APK.sh` | **推荐**的打包脚本：先建干净暂存目录 `_apk_stage/` 再构建 |
| `_extract_dsp.py` | 抽取脚本：改完桌面版算法后重新生成 `iqdsp.py` |
| `_smoke_dsp.py` | 算法层等价性测试（要求与桌面版**逐位相同**） |
| `_smoke_android.py` | 安卓版界面/数据链路冒烟测试 |
| `_shot_android.py` | 单页截图工具（一次进程截一页） |

**入口有两个名字，别混**：手机上跑（Pydroid / Termux / 直接 python）用
`main.py` 或 `iqplay_android.py` 都行，两者等价（`main.py` 只是转发）；
而 **buildozer 打包时必须是 `main.py`**，这是 p4a 的硬约定。

**算法只有一份**：`iqdsp.py` 是从 `iqplay0911.py` 抽出来的，不是手抄的。
改了桌面版的算法，跑一次 `python _extract_dsp.py` 就同步了。

---

## 二、三种运行方式

### 1) Pydroid 3（最省事，手机上直接跑）

1. 应用商店装 **Pydroid 3**，在它的 Pip 里装 `numpy` 和 `kivy`；
2. 把 `main.py`、`iqplay_android.py`、`iqdsp.py` 放进同一个目录
   （例如 `/storage/emulated/0/iqplay/`）；
3. 用 Pydroid 3 打开 `main.py` 点运行（打开 `iqplay_android.py` 也一样）。

> Pydroid 3 对 Kivy 的支持偶有问题（GL 后端）。跑不起来就走 Termux 或直接打包 APK。

### 2) Termux

```bash
pkg install python numpy
pip install kivy
cd /sdcard/iqplay
KIVY_WINDOW=sdl2 python main.py
```

### 3) 打包成 APK（要 Linux / WSL / macOS）

```bash
pip install buildozer cython
cd /path/to/b210chegnxu
bash 打包APK.sh                    # 推荐：先建 _apk_stage/ 干净目录再构建
#   产物：_apk_stage/bin/iqplay-3.2-arm64-v8a_armeabi-v7a-debug.apk
#   APK 装到手机后看日志： bash 打包APK.sh logcat

# 不想用脚本的话，需自己先把 main.py / iqplay_android.py / iqdsp.py /
# buildozer.spec 四个文件拷进一个空目录，再在该目录里：
#   buildozer -v android debug
```

**为什么要用脚本**：buildozer 会把 `source.dir` **整个**打包，而本项目根目录
里有几十个无关的 `.py`／`.cs16`／截图，直接打会把 APK 撑大、还可能混进同名模块。
`打包APK.sh` 只把该进包的三个文件 + spec 摆进 `_apk_stage/` 再构建。

首次构建会下载 Android SDK/NDK（3~5 GB，慢）。`buildozer.spec` 里已经配好
`android.api=34 / minapi=24 / ndk=25b`、存储权限和 arm64+v7a 双架构，
`iqdsp.py` 也一起打进包了。

**中文字体**：安卓系统的中文字体在 `/system/fonts/` 下，程序会自动找
（NotoSansCJK / DroidSansFallback 等）。要是你的机型都没匹配上，把任意中文
字体改名成 `font.ttf` 放在程序同目录即可。

---

## 三、界面与操作

底部五个页签：**频谱｜瀑布｜余晖｜时域｜调制**

- **触摸**：单指拖动 = 平移，双指捏合 = 缩放，双击 = 复位到自适应视图
- **顶栏**：`文件` 选 IQ（cs16/c16/wav/… 自动识别）；`自动XML` / `手动XML` 二选一
  - **自动XML**（默认）：选完文件立刻静默读同名 XML 的采样率，不弹窗，结果写在顶栏
  - **手动XML**：延续旧习惯，选文件时不读，点这个键才读
- **控制条**（可左右滑动）：播放/暂停/停止、速度、N_fft、载入点数、导出 PNG
- **载入点数**：大文件只读前面多少个点（默认 2M），手机上省内存的关键

---

## 四、与桌面版的差异

按需求**去掉了三个模块**（手机侧没有对应硬件或不适用）：

| 去掉的 | 原因 |
|---|---|
| B210 发射页 | 安卓没有 UHD 驱动 |
| URH 协议分析页 | 同上 |
| 信号源页 | 依赖 UDP 单向下发到外部信号源设备，手机侧没有该硬件 |

保留的能力：频谱（实时/平均/最大保持、占用带宽读数、Mark）、瀑布、余晖、
时域（含带通滤波）、调制域（星座图 + 自动识别 + AM/FM 解调监听）。

**桌面版 `iqplay0911.py` 不受影响**，三个模块在桌面版里都还在。

---

## 五、验证

```bash
# 算法层：与桌面版逐位一致 + 确认没把 tkinter/matplotlib 拉进来
python _smoke_dsp.py              # 目前 22/22

# 界面层：78 项断言（构建/切页/载入/播放/瀑布纹理/触摸/单位/时域/调制/空数据）
python _smoke_android.py          # 需要 kivy + numpy，目前 78/78

# 单页截图（桌面上看界面长什么样）
python _shot_android.py spectrum  _shot_spectrum.png  E:/NEW/signalwave/某文件
```

`_smoke_android.py` 在桌面跑时会遇到三个环境限制，测试里已经绕开并注释说明：

- Kivy 桌面窗口没有输入事件时几乎不重绘，所以**逐页截图不可靠**（五张图字节全同）
  ——改用 `_shot_android.py` 每页单独跑一次进程；
- 主循环被节流（`schedule_interval(0.12)` 实测 30s 才跑 1~2 拍），所以等待条件
  放在步骤**执行前**，并且不靠实时播放帧率推进，直接调 `app._tick()`；
- 切页后 `do_layout()` 还没算完，面板宽度是 0，折线抽稀会按"只有几像素"来算，
  于是"像素点串长度"的断言失去意义 —— 断言前先显式 `do_layout()`。

另外注意：**造测试 IQ 时别把文件头若干样本置零**。`draw_static()` 取的是
`iq[:N_fft]`，文件头全零会让静态频谱只剩底噪，Y 轴自适应退化成 10 dB 的
平噪声窗口，把"dB 量程有没有生效"这类断言测成假失败。要挖静默段请放在中段。

---

## 六、常见问题

**界面汉字是方框** → 没找到中文字体。把 `font.ttf`（任意中文字体改名）放到程序同目录。

**选完文件没反应 / 提示读不到 XML** → 安卓 11+ 需要存储权限，首次运行要授权；
XML 要和 IQ 同名同目录（`foo.cs16` 配 `foo.cs16.xml` 或 `foo.xml` 都认）。

**打开大文件卡** → 把控制条里的「载入点数」调小（0.5M/1M）。

**改算法后两边不一致** → 算法源头是 `iqplay0911.py`，改完记得跑 `python _extract_dsp.py`，
再跑 `_smoke_dsp.py` 确认逐位一致。
