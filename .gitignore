# ---------------------------------------------------------------------------
# 只为"打安卓 APK"准备的最小仓库
#
# 策略：**默认全部忽略，只放行该进包的文件**。
# 本项目目录里有几十个无关的 .py、几百 MB 的 cs16 / png，直接提交会把仓库
# 撑爆、还可能混进同名模块。
# ---------------------------------------------------------------------------
*

# ---- 打 APK 必需（buildozer.spec 的 source.dir 只认这四个） ----
!main.py
!iqplay_android.py
!iqdsp.py
!buildozer.spec

# ---- APK 里的图标与中文字体兜底（没有也不影响构建） ----
!*.ttf
!*.ttc
!icon.png
!presplash.png

# ---- 云端构建配置与文档 ----
!.github/
!.github/**
!.gitignore
!安卓版使用说明.md
!打包APK.sh

# ---- 抽取脚本：算法层的唯一来源，必须留（否则改了桌面版没法同步） ----
!_extract_dsp.py
